<div class="home1 hidden-xs">
    <% @categories.each do |c| %>
      <%= link_to category_path(c), class: "link-to" do %>
      <% image = Image.find(c.image_id) %>
    <div class="home-page-blok home-blok" style="background-image: url('<%= asset_path image.name %>')" >
      <div class="home-tekst-blok">
        <%= c.name  %>
      </div>
    </div>
    <% end %>
  <% end %>
</div>
<div class="hidden-sm hidden-md hidden-lg">
  <% @categories.each do |c| %>
    <%= link_to category_path(c), class: "link-to" do %>
      <% image = Image.find(c.image_id) %>
      <div class="mobile-blok home-page-blok" style="background-image: url('<%= asset_path image.name %>')">
      <div class="home-tekst-blok">
        <%= c.name  %>
      </div>
      </div>
    <% end %>
  <% end %>
</div>
<div class="banner banner-h">
<h4 class="resp1">Landing page title</h4>
<h3 class="resp2">Lorem ipsum dolor sit amet, consectetur adipisicing elit.</h3>
</div>
<div class="container-fluid hidden-xs">
  <div class="row-fluid">
    <% @categories.each do |c| %>
      <%= link_to category_path(c) do %>
        <div class="container-blok col-sm-6 col-md-4">
          <div class="inner-blok home-page-blok">
            <%= c.name %>
          </div>
        </div>
      <% end %>
    <% end %>
    <div class="container-blok col-sm-6 col-md-4">
      <div class="inner-blok home-page-blok">
        About BOEKJE voor JOU
      </div>
    </div>
  </div>
</div>
<div class="banner-mobile home-page-blok hidden-sm hidden-md hidden-lg">
  Create your own bookpresent<br/> This is how it works
</div>
<div class="hidden-sm hidden-md hidden-lg">
  <% @categories.each do |c| %>
    <%= link_to category_path(c) do %>
      <div class="mobile-blok home-page-blok">
        <%= c.name  %>
      </div>
    <% end %>
  <% end %>
  <div class="mobile-blok home-page-blok">
    About BOEKJE voor JOU
  </div>
</div>

